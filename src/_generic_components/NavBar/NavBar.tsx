import React from 'react';

import { LogoPanel } from '@/_generic_components/LogoPanel';

export function NavBar() {
    return (
        <nav>
            <LogoPanel></LogoPanel>
        </nav>
    );
}